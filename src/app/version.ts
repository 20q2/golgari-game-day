// Human-readable build tag shown in the navbar so players (and you) can confirm
// at a glance which client build a device is running.
//
// BUMP THIS on every deploy — especially last-minute ones during a game day.
// Format is just `YYYY-MM-DD-<n>`; increment <n> for each same-day redeploy.
export const APP_VERSION = '2026-08-08-1';
